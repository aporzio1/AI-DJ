# Patter — Project Tracker

**Owner:** Andrew P
**PM Agent:** `patter-pm` (see `.claude/agents/patter-pm.md`)
**Last updated:** 2026-07-05 — **Codebase-improvement-pass, Phases 0/1/2 all shipped and device-verified on branch `refactor/codebase-improvement-pass`** (plan: `docs/superpowers/plans/2026-07-05-codebase-improvement-pass.md`) — see §2. Phase 2 (testability + coverage) closed in full: all 7 planned items shipped, none skipped/descoped, 35→106 tests (plan estimated ~60). **Environment correction:** the plan's "Execution-environment caveat" (Linux container, no Xcode, "tests green" = unverified structure) does **not** describe the session that actually did Phase 0/1/2 work — that session ran on Andrew's real Mac with working Xcode, and every commit (including the Phase 1 follow-up `8794495`) was verified via real `xcodebuild test -scheme Patter -destination 'platform=macOS'` runs with actual pass counts, plus a closing `xcodebuild build -scheme Patter -destination 'generic/platform=iOS Simulator'` confirming the `#if os(iOS)` paths compile. Future sessions should not assume "tests green" here means unverified. **K35 is now CLOSED** — Andrew's physical-iPhone smoke confirmed both gate criteria: real render latency 0.81s (symptom A fixed) and clean MusicKit polling with no IPC disruption (symptom B stays fixed). **Phase 1 is fully closed.** Same smoke session surfaced and fixed a new bug — the K6/K24 Kokoro CoreML guard didn't cover iOS 27 (exact-match `== 26` check), causing an app hang; fixed and device-verified same session (`aaec296`, closed as K37). It also surfaced a new, unaddressed bottleneck — DJBrain's Foundation Models generation call (~4.3s) is the dominant remaining DJ-segment startup cost, larger than the now-fixed render slice (K38, open, deferred). **Phase 3 (Settings restructure) and Phase 4 (macOS sandbox spike) not started** — both gated on human-perceptual verification that Xcode access alone doesn't satisfy (see §3). Prior close-out (2026-05-01): K33 fixed, K34 documented-not-eliminated, both closed same day.

---

## 1. Current State

- **Platforms:** iOS 26 / macOS 26, Apple Silicon only
- **Stack:** Swift 6.0 strict concurrency, SwiftUI, MusicKit, AVFoundation, Foundation Models
- **Version:** 1.0 (CFBundleVersion 1)
- **App name:** Patter (renamed from "AI DJ" on 2026-04-23). Bundle ID `com.andrewporzio.patter`. Module name `Patter`. Source folders `Patter/` + `PatterTests/`. **App Store Connect listing name: "Patter: Your DJ"** — bare "Patter" was reserved by another developer in Apple's broader name pool (not visible in App Store search, common for pre-launch reservations). Home-screen `CFBundleDisplayName` stays as just "Patter" — listing name only shows in App Store search/store page. Top-level CloudDocs working dir is still `AI DJ/` (not yet renamed — leave for a clean session to avoid disrupting active Claude Code state and the auto-memory path). Spec doc filenames retain the original `ai-dj` codename (historical record).
- **Core capability:** Plays Apple Music content (playlists, albums, stations) with an AI DJ narrating between tracks; can pull RSS news headlines for commentary
- **TTS providers (pluggable via `DJVoiceRouter`):** Device Voices (AVSpeechSynthesizer), OpenAI cloud TTS, Kokoro on-device (FluidAudio CoreML), all with launch-time warm-up (`RootView.handleReady`) — System voice's warm-up landed 2026-07-05 (`bc9a871`), completing parity with DJBrain/Kokoro, and is now device-verified (K35 closed, §6). **Default provider is `.system`** (`SettingsViewModel.ttsProvider = .system` at declaration); OpenAI is never auto-selected. System path: sentence-chunked rendering, per-script timeout, premium-quality preference (premium > enhanced > default with US English + name preference scoring), one long-lived `AVSpeechSynthesizer` reused across renders with `usesApplicationAudioSession = false` on iOS (detached from the shared session so per-render AU activations don't disrupt `ApplicationMusicPlayer` — K35), plus an iOS-26**+** Kokoro auto-downgrade `.kokoro → .system` to dodge the CoreML compile hang (see K6/K24/K37 — guard widened from exact-match `== 26` to `>= 26` after an iOS 27 device hit an uncovered gap).
- **Music providers:** Apple Music only. `Track.MusicProviderID` is a single-case enum (`.appleMusic`). `MusicProviderRouter` remains as a single-provider wrapper (`init(appleMusic:)`) so a future provider with an actual streaming SDK can slot in without re-threading the coordinator + VM call sites. `MusicProviderService` protocol, `ProviderAuthStatus`, `ProviderArtwork`, `PlayableItem` provider-namespaced IDs, and `LibrarySectionCache` provider-scoped keys all retained intentionally.
- **UI tabs:** Library (default) / Queue / Settings, plus persistent MiniPlayerBar (2-row with progress slider, shuffle/repeat, thumbs feedback) and an expandable Now Playing sheet. Library has no segmented provider picker (single provider).
- **Library landing:** Recently Played + Made for You (playlists, albums, stations) with stale-while-revalidate disk cache + pull-to-refresh. All tiles tap-to-play.
- **Settings → Music Services:** reduced to a single "Apple Music: Authorized" status line.
- **Onboarding:** 4-step first-launch wizard (Name → DJ + Voice → News → iCloud) with migration auto-completion for existing users
- **iCloud sync:** `CloudSyncService` mirrors curated Settings keys to NSUbiquitousKeyValueStore; opt-in toggle + hot-reload across devices
- **Personas:** Multiple personas with built-in presets + user-created custom personas; DJ frequency + News frequency pickers
- **Feedback:** Thumbs-up / thumbs-down per track injected into DJ prompt

---

## 2. Shipped

Reverse-chronological. Commit hashes are 7-char.

### 2026-07-05 — Codebase-improvement-pass, Phase 2 (testability + coverage)

Branch `refactor/codebase-improvement-pass`, plan `docs/superpowers/plans/2026-07-05-codebase-improvement-pass.md`. All 7 planned items shipped, none skipped or descoped — closes Phase 2 in full. 35 → 106 tests (plan estimated ~60; actual came in higher).

**Environment correction:** contrary to the plan's "Execution-environment caveat" (which assumed a Linux container with no Xcode), this session ran on Andrew's Mac with working Xcode/`xcodebuild`. Every commit below — plus the Phase 1 follow-up (`8794495`, logged in the section below) — was verified with a real `xcodebuild test -scheme Patter -destination 'platform=macOS'` run showing actual pass counts, not just structured for later verification. A closing `xcodebuild build -scheme Patter -destination 'generic/platform=iOS Simulator'` also succeeded, confirming the `#if os(iOS)` paths (notably `SettingsViewModel`'s iOS-only branches, never compiled by the macOS test run) build clean. Future sessions should not assume "tests green" in this pass means "unverified structure" the way the plan's caveat implies.

- `b3ecf36` — **`SettingsViewModel` gets `init(defaults: UserDefaults = .standard)`**, threaded through save/load/`applyIOS26KokoroDowngradeIfNeeded`/`migrateLegacyPersonaIfNeeded`. New `SettingsViewModelTests.swift` (`@MainActor`) pre-sets the `openAIKeychainMigratedToSynchronizable` sentinel so tests don't touch the real Keychain. Covers feed add/remove/dedup, OPML import dedup (against-existing and within-same-import), persona CRUD (add/duplicate/update/delete, built-in no-op guard, active-ID fallback, unresolvable-ID fallback), legacy-persona migration (matches-Alex-drop path and differs-preserve path), the iOS 26 Kokoro downgrade's macOS no-op path (iOS branch itself untestable off-device — smoke-only), and `effectiveVoiceIdentifier`'s persona-preset fallback. 85→106 tests.
- `60787ed` — **`RSSFetcher.fetchHeadlines()` fans out per-feed via `withTaskGroup`** instead of a sequential loop — one slow feed no longer blocks the others; each task catches its own error so a bad feed fails soft. Added an HTTP status check in `fetch(_:)` — non-2xx now throws `RSSFetchError.badStatus` instead of trying to parse an error body as XML. Hoisted `FeedParser`'s RFC 2822 / ISO 8601 date formatters from per-item construction to per-parse instance lets. **Real behavior change worth tracker visibility:** cross-feed dedup of the exact same URL is no longer deterministic about which feed's copy wins, since task completion order isn't guaranteed under concurrency — accepted, since duplicates are expected to be near-identical and the new tests assert dedup *count* only, never which title survives (see K36). New "RSSFetcher network" suite (MockURLProtocol-backed, `.serialized` trait since the mock's response map is shared static state) covers multi-feed merge/sort, cross-feed dedup, one-feed-failing-soft-while-others-succeed, all-feeds-failing-returns-empty. 81→85 tests.
- `77a5fb8` — **Fakes hygiene.** `FakeDJBrain` and `FakeDJVoice`/new `FakeKokoroDJVoice` converted from `@unchecked Sendable` to real `@MainActor` isolation — legitimate conformance, since their protocols are fully async, not just a style change. **Plan deviation worth flagging:** the plan assumed all three fakes (including `FakeRSSFetcher`) could make this conversion because "all their protocol requirements are async" — that premise was wrong for `FakeRSSFetcher` specifically: `RSSFetcherProtocol.updateFeeds` is a non-async, `nonisolated` requirement that a MainActor witness can't satisfy for nonisolated callers. `FakeRSSFetcher` stays `@unchecked Sendable` — only 2 of the claimed 3 fakes converted. Also replaced hardcoded shared `/tmp/fake.caf` / `/tmp/seg.caf` literals with unique per-instance temp URLs, and added `startError`/`songsInPlaylist` knobs to `FakeMusicService`. No new tests, hygiene only — stayed at 81.
- `74aac84` — **`DJVoiceRouter`'s designated init now takes `system`/`openAI` as `any DJVoiceProtocol`** and `kokoro` as `any DJVoiceProtocol & KokoroModelManaging` (new 2-method protocol, adopted by `KokoroDJVoice`) instead of concrete types; concrete defaults preserve `PatterApp`'s no-arg call site. `setOpenAIModel` downcasts to `OpenAIDJVoice` (model selection is provider-specific). New `DJVoiceRouterTests.swift` covering routing, provider switching, OpenAI/Kokoro-failure-falls-back-to-System-with-empty-voice-ID, System-failure-propagates-without-fallback, Kokoro model-management proxy, and the K35 `warmUpSystemVoice` proxy. 70→81 tests.
- `b06f68b` — **Extracted `DJPromptTemplate`** (instructions block, `buildPrompt`, `cleanTitle`) out of `DJBrain` into a new stateless enum. Producer's canned-fallback script now uses `DJPromptTemplate.cleanTitle` too — previously it used the raw uncleaned title, unlike DJBrain's own empty-script fallback, a small pre-existing inconsistency this closes. `buildPrompt` takes pre-resolved `newsTopic`/`newsSummary` strings rather than depending on DJBrain's `usableNewsContext`/`cleanHeadline` instance methods, keeping it fully decoupled. Hoisted the duplicated `https?://\S+` strip into one `stripURLs` helper on `DJBrain`. New tests for `cleanTitle`, `buildPrompt`'s conditional sections, instructions' persona/news-guidance branching. 53→70 tests.
- `57793fd` — **`LibrarySectionCache`'s load/save/clear get a `defaults:` param.** New `LibrarySectionCacheTests.swift`: roundtrip, section isolation, `Entry.isFresh` TTL boundary (confirmed exclusive — exactly-at-TTL already reads stale). 45→53 tests.
- `46e4460` — **`TrackFeedbackStore` gets `init(defaults: UserDefaults = .standard)`.** New `TrackFeedbackStoreTests.swift`: cap-at-50 FIFO, per-track rating, summary dedup/limits, `clear`, persistence roundtrip. 35→45 tests.

### 2026-07-05 — Codebase-improvement-pass, Phase 0 + Phase 1

Branch `refactor/codebase-improvement-pass`, plan `docs/superpowers/plans/2026-07-05-codebase-improvement-pass.md` (`3d296e5`).

- **Device verification (physical iPhone, iOS 27) — K35 closed, new bug found+fixed, new bottleneck filed.** Andrew ran two real device smokes this session:
  - `aaec296` — **Widened iOS 26 Kokoro guard to `>= 26` (closed as K37).** First smoke hit a hang on Andrew's iPhone: it was running **iOS 27** (not 26) with `ttsProvider` persisted as `.kokoro` from before an OS upgrade. Both existing guards (`SettingsViewModel.applyIOS26KokoroDowngradeIfNeeded()` and `DJVoiceRouter.swift`'s `TTSProvider.available`) checked `majorVersion == 26` exactly, so neither fired on 27. Kokoro's CoreML compile hard-failed with a new signature (MPSGraph/MLIR assertion, "axes in perm tensor must be unique" — distinct from the original K6/K24 libBNNS SME2 segfault) and hung the app. Since `RootView.handleReady` auto-warms Kokoro at every launch whenever `.kokoro` is persisted, this would have re-hung on every relaunch regardless of reinstall — only a code fix resolved it. Widened both guards to `>= 26`. Rebuilt, reinstalled, relaunched on the same device: guard fired correctly, no Kokoro compile attempt in the log, auto-downgraded to `.system`, `.caf` renders throughout, no hang. **Device-verified fixed.**
  - `9f591b7` — **Added K35 timing diagnostics to `SystemDJVoice`** (`renderToFile`/`warmUp`/`performRender`, tagged warmup vs render) — landed just before the fix above to get real numbers on the next run.
  - **K35 second smoke (post-Kokoro-fix, provider back to `.system`) — both gate criteria now satisfied, K35 closed:** `SystemDJVoice.renderToFile` (real, user-facing render) measured **0.81s** — sub-second, confirms the AU-handshake cost is being paid during the invisible launch warm-up instead of the live segment (symptom A, fixed). MusicKit polling ticked cleanly across the run with no `xpc service connection interrupted` / `prepareToPlay failed` — the K31/K35 symptom-B failure signature is absent (symptom B, stays fixed). `SystemDJVoice.warmUp` (the launch-time throwaway render) measured 6.57s, elevated but expected/by-design — it runs concurrently with DJBrain's own ~11-12s Foundation-Models warm-up under `.utility` priority, contending for CPU/AU resources at launch; this is invisible to the user and not a K35 regression. **Closes K35 in full** — see §6.
  - **New Known Issue filed (K38, open, deferred):** the same run measured `DJBrain.generateScript` (the real LLM call) at 4.3s — total DJ-segment-start-to-audio-ready is generation (4.3s) + render (0.81s) ≈ 5.1s. K35 only ever targeted the render slice; the generation slice is bigger and explains Andrew's subjective "I don't think it was quicker" read after the K35 fix landed. `DJBrain.warmUp()` (pre-existing, unrelated to K35) spent ~11-12s at launch but didn't meaningfully reduce the real call's cost. No diagnosis or fix attempted — Andrew explicitly chose to close K35 and file this for a future session rather than dig in now. See K38.
- `1fcc651` — Pinned `LastUpgradeCheck` to the installed Xcode version (build-config housekeeping, no behavior change).
- `8794495` — **Close TOCTOU race in `SystemDJVoice` render serialization.** `bc9a871`'s `serialized()` read the tail `Task` and wrote the new one under two *separate* `NSLock` acquisitions, so two concurrent callers (launch warm-up + a real Producer render) could both read the same `previous` tail before either wrote the new one — defeating the serialization `bc9a871` was written to guarantee. Fixed by moving the read+swap into one locked closure (`Task`'s initializer is synchronous — it schedules without awaiting — so no suspension point exists between read and write inside the lock). macOS suite green 35/35.
- `bc9a871` — **Warm `SystemDJVoice` at launch to skip first-utterance AU handshake (K35, layer 3).** Added `DJVoiceProtocol.warmUp(voiceIdentifier:)` (default no-op); `SystemDJVoice` implements it as a throwaway "Ready." render; `SystemDJVoice` renders are now serialized via a task chain so the detached launch warm-up can't overlap a real Producer render on the shared synthesizer; `DJVoiceRouter.warmUpSystemVoice` proxy added; `RootView.handleReady` fires it on a detached task when System provider is active — mirrors the existing DJBrain/Kokoro warm-up pattern. This targets the *first-utterance latency* symptom (distinct from the mid-track MusicKit-disruption symptom `9779040`/`d97ce8e` fixed on 2026-05-01 — see corrected K35 entry in §6). Requires iPhone smoke to confirm the gap closes and the warm-up render doesn't perturb MusicKit.
- Phase 0 — 9 quick-win commits, zero behavior change, macOS suite green throughout: `5c592e5` (removed dead `asTrack` extension, named recommendation cap), `d2e30cc` (corrected stale `DJVoiceProtocol` output-format doc), `17dc8ed` (removed force-unwrap on swipe-delete `IndexSet`), `6d06e84` (named track-advance timing constants in `monitorTrackUntilEnd`), `afd9feb` (split `shouldGenerate` into pure predicate + state mutation), `69a9366` (deduped repeated `voicePreset` string across built-in personas), `bfb6f4d` (deduped m:ss time formatter into `Utilities/TimeFormatting.swift`), `f4f6687` (deduped iOS-26 Kokoro provider filter into `TTSProvider.available`), `aa60eb4` (dropped redundant MainActor hops, cached voice enumeration).
- `c8d48a9` — **Expanded `CLAUDE.md`/`AGENTS.md`; archived stale AI DJ design spec.** Added service-level architecture, build gotchas (concurrency, CloudSync, Apple Intelligence device gate), tracker/PM pointers, cross-links between the two docs; marked `docs/superpowers/specs/2026-04-17-ai-dj-design.md` as archived with an explicit stale-content callout.

### 2026-05-01
- `d97ce8e` — **Detach DJ synth from shared audio session to stop disrupting MusicKit (K35 spike J, layer 2).** `9779040`'s warm-synth fix skipped the AU IPC handshake on renders 2+, but Andrew's iPhone re-test showed track playback still stalling at `willAdvance` — the load-bearing cost wasn't the handshake, it was the per-render AU *activation* touching the shared `AVAudioSession` on every `write()` call, regardless of synth lifetime. Set `usesApplicationAudioSession = false` on the long-lived synth (iOS-only, `#if os(iOS)`) so it gets a private AU session; costs nothing audible since rendering is in-memory PCM via `write(_:toBufferCallback:)`, not through-speaker playback. Warm-synth optimization from `9779040` retained — both fixes stack. 35 tests pass on macOS; iPhone re-test was pending at commit time.
- `9779040` — **Reuse warm `AVSpeechSynthesizer` across renders to stop disrupting MusicKit (K35 spike F, layer 1).** Producer's next-up DJ render (~30s before track end) constructed a fresh `AVSpeechSynthesizer` per call; the synth's first-use AU IPC handshake transiently disrupted the shared `AVAudioSession`, which on iOS pre-empted `ApplicationMusicPlayer` and froze the currently-playing track for ~10s at every track-end transition. Hoisted the synthesizer to a long-lived property on `SystemDJVoice`, injected into `SpeechRenderer`. **Turned out insufficient on its own** — see `d97ce8e` same day. Both commits were shipped 2026-05-01 but neither had been logged in this tracker until the 2026-07-05 pass; see corrected K35 entry in §6.
- `a6a832a` — **Document AudioGraph `stop()` residual race + `nonisolated(unsafe)` justification (K34).** Docs-only. Reviewer's "capture continuation at stop-call time" suggestion turned out to be impossible: `stop()` is sync (driven by `withTaskCancellationHandler.onCancel`, which doesn't permit async), so any actor-state read happens *inside* the spawned Task — same race the suggestion was trying to close. Documented the residual race window: at most a too-early resume of a new `play()` (~50 ms audible skip), not data corruption; `resumePending()`'s atomic read+nil ensures no double-resume. Race requires user-tap-pause + new-play in the same actor turn — not a natural user flow. Added a comment block above the `nonisolated(unsafe)` annotations explaining why the suppression is justified, plus a standing rule for future AudioGraph touches to re-validate the three preconditions. A real fix would require either (i) async `stop()` with large blast radius or (ii) per-`play()` UUID-keyed continuation slots (contract redesign); both rejected for now, revisit if observed in production logs. **Closes K34 as documented-not-eliminated.**
- `c87965f` — **Move `playbackGeneration` bump to call sites; monitor stops self-bumping (K33).** Extracted a `private func startNewGeneration()` helper. Three play-initiation sites now bump generation before invoking the monitor: `playTrack`, resume-from-pause in `play()`, and `playSegment`. `monitorTrackUntilEnd` no longer self-bumps; it captures the value its caller already set. Five transport-interruption sites (`replaceQueue`, `startStation`, skip, previous, `swapCurrentSegment`) keep their inline `playbackGeneration += 1` per Andrew's guidance. `playbackGeneration` is now `private(set)` so the test target can observe. Added `playbackGenerationContract` test (35 tests pass on macOS, was 34). **Closes K33.**

### 2026-04-30
- `769bbcf` — **Declare `UIBackgroundModes: audio` so DJ TTS keeps speaking when backgrounded (K32).** Added `UIBackgroundModes: [audio]` to `project.yml`'s `info.properties` block; `xcodegen generate` propagated through to `Patter/Resources/Info.plist`. Apple Music tracks were unaffected by backgrounding (they render out-of-process via musicd) but DJ TTS pipeline through AVAudioEngine is in-process and was being suspended by iOS without the entitlement. One-line manifest change; macOS test suite still green at 32. Side note: the `CFBundleVersion` auto-bump XcodeGen does on every regen (142 → 157 this round) was incidentally included in this commit — going forward we let the bump land with the change rather than excluding it. **Closes K32.**
- `c9788f0` — **Revert scenePhase reactivation; iOS MusicKit playback restored (K31).** `RootView.swift` no longer declares `@Environment(\.scenePhase)` and drops the iOS scenePhase observer. `AudioGraph.swift` removes `nonisolated reactivate()` + `private nonisolated configureSession()` and inlines the one-time iOS session setup back into `init()` with a K31 comment. Net surface smaller than what K25 originally shipped. Andrew's iPhone re-smoke confirmed: tracks play after the first DJ news segment (no `xpc service connection interrupted` → `prepareToPlay failed`) AND volume holds across foreground transitions (so K25's user-visible bug stays fixed by the `.duckOthers` removal alone). 32 tests pass on macOS. **Closes K31; K25 stays closed.**
- `07e1844` — **Route Kokoro `initialize()` through ManagerBox for Swift 6 sendability.** Build follow-up to K26's picker-disable / Kokoro plumbing changes; resolves a Swift 6 sendability complaint in the Kokoro initialization path.
- `885e825` — **Disable macOS app-sandbox to restore MusicKit IPC.** Originally framed as the K30 fix; later (same day) reclassified as harmless since the K31 disambiguation showed Andrew's IPC errors were on iPhone, not macOS. The rollback stays — `app-sandbox` is a no-op on iOS and re-enabling it for Mac App Store submission requires a separate mach-lookup exception audit. Other parts of `eb14ec0` (deployment targets, app category, DocC gitignore) retained.
- `ce67651` — **Surface news article link on Now Playing (N1.3).** When a `.news` DJ segment is currently playing, NowPlayingView shows a "Read article" button that opens the headline URL via `Environment(\.openURL)`. Adds `sourceHeadline: NewsHeadline?` field to `DJSegment`; `Producer.generateSegment` populates it; three test constructor sites updated. **Closes the N1 arc** (N1.1 dedup + N1.2 richer briefing previously shipped).
- `1c414c8` — **iPhone smoke regressions (K25–K29).** Bundled fix landing the original K25 (`.duckOthers` removal + scenePhase observer — observer later reverted via K31), K26 (Kokoro picker disabled on iOS 26 with footer copy), K27 (shuffle as persisted mode toggle, mirroring repeat-mode pattern), K28 (Playlist detail Play button rewritten with explicit `HStack`), K29 (premium-voice nudge lists both Spoken Content + VoiceOver paths; deep-link tries `Content_Speech` then VoiceOver pane then Accessibility root).

### 2026-04-26
- `24ecdf0` — **iOS premium-voice nudge in onboarding wizard + Settings.** When the device has no Premium English voice installed, both surfaces show a step-by-step nudge to download one from iOS Settings → Accessibility → Spoken Content → Voices. Text-only (iOS has no public deep-link API for that pane). Detection conditional on `quality == .premium` so the nudge auto-hides once installed. Pairs with `fd5f397` so first-launch on a fresh iPhone sounds materially better without any neural-TTS dependencies. **Closes Path X / D13.**
- `fb01c1f` — **Suggested RSS feeds extracted + surfaced in Settings.** New `Patter/Models/SuggestedRSSFeed.swift` with stable URL-derived id; `PreferencesWizardView` migrated off the inline `SuggestedFeed` type; Settings → News gains a "Suggested Feeds" section with the same toggle pattern as the wizard.
- `0bd940b` — **iCloud Keychain sync for OpenAI API key.** Keychain set/get/delete now use `kSecAttrSynchronizable`. New `migrateToSynchronizable` helper called from `SettingsViewModel` on load. OpenAI footer copy updated to reflect sync. **Closes Backlog #9.**
- `fd5f397` — **System voice quality + DJ pipeline tuning.** `SystemDJVoice` now renders sentence-chunked utterances with quality-aware voice picking (premium > enhanced > default, US English + name-match boost), per-script timeout (20–60s vs flat 15s), and a new `noAudioRendered` error case. `PlaybackCoordinator` widened `willAdvance` lead time from 20s to 35s. `Producer` + `RootView` pass voice identifier verbatim. Voice picker label "System Default" → "Best Available Device Voice" in both wizard + Settings. **Closes Backlog #6 path (c) for iOS Kokoro compile hang via the auto-downgrade.**
- `eb14ec0` — **TestFlight metadata: deployment targets, sandbox, app category.** Pinned `IPHONEOS_DEPLOYMENT_TARGET` and `MACOSX_DEPLOYMENT_TARGET` to 26.0 in `project.yml`, enabled `com.apple.security.app-sandbox` in `Patter.entitlements`, added `LSApplicationCategoryType=public.app-category.music` to Info.plist, and added `Patter.doccarchive/` to `.gitignore` — recovered ~22,000 stray DocC build artifacts that had been accidentally tracked. *(2026-04-30: app-sandbox portion partially rolled back per K30 — broke macOS MusicKit IPC. Other parts of this commit retained.)*
- `c4206cc` — **K23 verdict + Path X plan committed.** Tracker logged the GPL-3 contamination finding for sherpa-onnx + Piper VITS, locked Path X as the next move, demoted bundled TTS to Path Y (system-voice extension, deferred).

### 2026-04-23
- **App rename: "AI DJ" → "Patter".** Researched 6 candidate names against App Store collisions, trademark exposure, domain availability; "Patter" won on concept fit (it's the radio-DJ term for between-song chatter — the app's signature feature) and clean availability (zero music apps in the App Store, no `.app`/`.fm` squatters, no obvious TM heat in software/music). Folder rename: `AIDJ/` → `Patter/`, `AIDJTests/` → `PatterTests/`, `AIDJ.entitlements` → `Patter.entitlements`, `AIDJApp.swift` → `PatterApp.swift`, `aidj-pm.md` → `patter-pm.md`. Code edits: module qualifier `AIDJ.Track` → `Patter.Track` across 11 files; `@testable import AIDJ` → `@testable import Patter` across 5 test files; `@main struct AIDJApp` → `PatterApp`; comment refs to `AIDJApp` updated. Identifier edits: bundle ID `com.andrewporzio.aidj` → `com.andrewporzio.patter`; Log subsystem + Keychain service-id fallback aligned. UI strings: `CFBundleDisplayName`, `NSAppleMusicUsageDescription`, `NavigationStack` title, onboarding gate copy, settings reset-confirmation copy. project.yml fully rewritten (target name, scheme name, sources path, entitlements path, info path, TEST_HOST). `xcodegen generate` produced `Patter.xcodeproj`; old `AI DJ.xcodeproj` removed. `OpenAIDJVoice` deliberately preserved (it's "OpenAI" + "DJVoice", not "AIDJ"). 25 tests pass on macOS, iOS Simulator builds green. Process: GitHub repo rename + Apple Developer Portal + App Store Connect setup are user actions (see §3).

### 2026-04-22
- `ca23170` — **Spotify integration dropped; Apple Music only (Option B).** 2,141 lines deleted, 61 added. Removed `SpotifyAuth.swift`, `SpotifyAPIClient.swift`, `SpotifyService.swift`, `SpotifyAPIClientTests.swift`, `SpotifyAuthCoordinatorTests.swift`; removed SpotifyiOS SPM package, `aidj://` URL scheme, `LSApplicationQueriesSchemes` from `project.yml`. `Track.MusicProviderID` now single-case (`.appleMusic`); `MusicProviderRouter` is `init(appleMusic:)`-only; `MusicProviderService` protocol dropped `handleAuthCallback(_:)` and `validateAuthorization()`. Settings "Music Services" reduced to status line; Library lost segmented picker. Router abstraction + provider-neutral types retained as future scaffolding. 25 tests pass, green on macOS + iOS. Rationale locked in K21.

### 2026-04-21
- `3c968fc` — Phase 2b playback gate surfaces a dismissible alert on Spotify track tap. *(Withdrawn with `ca23170` — see §8.)*
- `2854cd6` — `validateAuthorization()` probe on Settings appear to clear stale "Connected" state. *(Withdrawn.)*
- `4b5e97b` — **macOS Spotify auth pivot** — swapped `ASWebAuthenticationSession` for `NSWorkspace.open(_:)` + `.onOpenURL` on macOS (K16 workaround). *(Withdrawn.)*
- `564037f` — Diagnostic: log ASWebAuthenticationSession presentation anchor. *(Withdrawn.)*
- `a2c1516` — Diagnostic: trace `beginAuthFlow()` + retain session past method return. *(Withdrawn.)*
- `51b1080` — Capture presentation anchor on MainActor for PKCE. *(Withdrawn.)*
- `8d926fd` — First (unsuccessful) attempt at libdispatch crash fix. *(Withdrawn.)*
- `688bbf8` — Phase 1 smoke-test regression fixes. **Kept** — two real `PlaybackCoordinator` bugs unrelated to Spotify (K15 invariants).
- `10fc7f3` — Phase 2a.5: Library segmented provider picker. *(Withdrawn.)*
- `8a1eeee` — Phase 2a.4: `browseProvider` persistence + Settings Music Services row. *(Withdrawn.)*
- `ac8a5ed` — Phase 2a.3: `SpotifyService` conforming to `MusicProviderService`. *(Withdrawn.)*
- `c9b0583` — Phase 2a.2b: real Spotify Client ID. *(Withdrawn.)*
- `9dd5d39` — Phase 2a.2a: `SpotifyAPIClient` actor + URLProtocol tests. *(Withdrawn.)*
- `d69eadc` — Phase 2a.1: Spotify PKCE auth coordinator + Keychain keys. *(Withdrawn.)*
- `c325b56` — K1 closed: test suite green after Info.plist + main-actor + stale-assertion fixes. **Kept — unrelated to Spotify.**
- `3d09898` — Phase 1c: introduced `MusicProviderRouter` (`@MainActor final`). **Kept — retained post-rip as future-provider scaffolding.**
- `8bb13d3` — Phase 1b: provider-neutral `ProviderArtwork`. **Kept.**
- `79df4af` — Phase 1a: renamed `MusicKitServiceProtocol` → `MusicProviderService`; `PlayableItem.id` namespaced by `providerID`. **Kept.**
- `686a818` — Tracker sweep.
- `11ca3ca` — Station playback now shows track info + transport works.
- `48b31bf` — Warm up Kokoro at launch; MiniPlayerBar indicator distinguishes "Loading" from "Downloading".
- `b8a0f95` — `replaceQueue` now stops in-flight playback so station switches don't overlap.
- `3eb85b0` — Wire station playback so Made for You cards are all tappable (playlists, albums, stations).
- `5c09db9` — Don't let empty cache poison Made for You for 30 min.
- `7bb16a2` — Made for You surfaces albums + stations, not just playlists.
- `1c5dfed` — Cache Recently Played + Made for You with stale-while-revalidate.
- `a432220` — Spell out initialisms (GPT, NPR, BBC, etc.) so TTS doesn't say "gept".
- `5eb5828` — Download indicator condition fix.
- `125b685` — Download indicator now actually appears + tone down AI-flowery DJ.
- `efa3934` — MiniPlayerBar Kokoro download indicator.
- `846252f` — Merge wizard DJ + Voice steps into a single page.
- `80b2f9f` — Reset Onboarding now actually shows the wizard next launch.
- `262566f` — Onboarding wizard now includes voice provider + voice.
- `d08228b` — Reset Onboarding now confirms + tells you it worked.
- `dfa6691` — Onboarding Commit B: first-launch 4-step wizard.
- `dd6ff07` — Onboarding Commit A: iCloud sync for Settings + RSS feeds.
- `b154f0b` — Stop hiding persona name from VoiceOver on Settings row.
- `664046b` — iOS tab bar was obscured by MiniPlayerBar — move inset per-tab.
- `05303a9` — iOS full-screen launch + orientations + Kokoro sending warning fix.
- `5804abd` — Stop DJ hallucinating stations + looping one headline.
- `9e38a5c` — Stop double-gating news.
- `fb70f38` — Pause then play resumes in place instead of restarting track.
- `fe0cc65` — Thumbs on MiniPlayerBar (Phase 3 of MiniPlayerBar work).
- `ee48a67` — News frequency slider.
- `a7afe2a` — News pipeline diagnostics.
- `c652952` — Persist DEVELOPMENT_TEAM in project.yml.
- `2fdaaf4` — HIG P2 — normalize off-grid spacing.
- `bfad5e5` — HIG P1 — tap targets, a11y labels, destructive confirmation.
- `2134b41` — Thumbs-up / thumbs-down track feedback.
- `a63e513` — MiniPlayerBar shuffle + repeat controls.
- `66ad02e` — MiniPlayerBar progress slider + 2-row layout.
- `68a67d7` — DJ frequency setting.
- `eb808d6` — Multiple DJ personas with built-in presets.
- `8705b17` — Editable DJ persona — name + instructions.
- `72246a0` — Register AppIcon for macOS.
- `2b00f39` — Cmd+, opens Settings in a separate window on macOS.
- `d0e29a7` — Skip `playerNode.stop` on same-format segments.
- `844dfac` — Library landing page — Recommendations section (Phase 2).

### 2026-04-20
- `290263b` — Library landing page — Recently Played section (Phase 1).
- `00041d3` — Grounded PM agent in PMI Talent Triangle + HBR project-management principles.
- `600ffae` — Added AI DJ project-manager agent and tracker.
- `c42dc1d` — Playlist detail view + shuffle button + OpenAI voice descriptors.
- `946181a` — Spotify support implementation plan committed (docs only). *(Plan withdrawn 2026-04-22 — see §7.)*
- `9fd34cf` — Kokoro voice preview in Settings; trash-can delete on RSS rows.
- `423b27a` — Redesigned Add Feed row.
- `760e17e` — Manual Download and Remove Model buttons for Kokoro TTS.
- `0ad3c8a` — Phase 2: Kokoro on-device TTS via FluidAudio.

### 2026-04-17
- `0f4422c` — `willAdvance` lead time widened; softened news-hook prompt.
- `3c4df88` — Decode audio into PCM buffer for reliable MP3 playback from OpenAI.
- `2c7a795` — Phase 1: pluggable TTS provider layer; added OpenAI TTS.
- `01022cf` — Deferred `playerNode.stop` to eliminate priority inversion.
- `5193ca1` — DJ no longer announces an unplayable track.
- `6a3a3bb` — Three QoL fixes from user logs.

### 2026-04-16 and earlier
- `84cf31c` — Step-by-step voice-download instructions in Settings footer (macOS).
- `15f9908` — macOS button that opens System Settings → Spoken Content directly.
- `c2d5a72` — Eliminated priority inversion in AudioGraph stop path.
- `da5ffdc` — Voice picker in Settings.
- `1b726b6` — Progress line across top of MiniPlayerBar.
- `7ff8942` — Persistent MiniPlayerBar at bottom; Library default tab.
- `a790a18` — Library search: filter playlists and play catalog songs.
- `95dc898` — Removed "e.g. Andrew" placeholder.
- `0cec98d` — HIG pass on SettingsView.

---

## 3. In Progress

### Codebase-improvement-pass (2026-07-05, branch `refactor/codebase-improvement-pass`)

Plan: `docs/superpowers/plans/2026-07-05-codebase-improvement-pass.md`. Owner: Andrew + Claude.

- **Phase 0 (quick wins)** — shipped, 9 commits, zero behavior change. See §2.
- **Phase 1 (K35 launch warm-up)** — **shipped and fully closed.** Code shipped (`bc9a871`, `8794495`), macOS suite green, and now device-verified on Andrew's physical iPhone: `renderToFile` 0.81s (symptom A fixed) + clean MusicKit polling with no IPC disruption (symptom B stays fixed). K35 closed (§6). Same smoke session also surfaced and fixed an unrelated hang (K37, closed) and filed a new, larger, unaddressed bottleneck (K38, open, deferred) — see §2 device-verification entry and §6.
- **Phase 2 (testability + coverage)** — **shipped in full, all 7 items, none skipped or descoped.** 35 → 106 tests (plan estimated ~60; actual came in higher). See §2 for the commit-by-commit breakdown, the environment correction, the `FakeRSSFetcher` plan-deviation note, and the RSS dedup-nondeterminism behavior change (K36).
- **Phase 3 (Settings restructure) and Phase 4 (macOS sandbox spike) — not started.** Both remain gated on human-perceptual verification the plan calls for explicitly: Phase 3 needs a "manual macOS Settings run-through by Andrew," Phase 4 needs a full macOS app smoke (Apple Music auth, Kokoro download, Keychain, iCloud KVS). Neither is satisfied by this session having real Xcode/`xcodebuild` access — the gate is Andrew's own eyes on the running app, not a build or test result. Phase 2's tests-as-safety-net property doesn't carry over to view-layer restructuring. Stopping here per the plan's own phase-dependency shape.

*(All prior review-driven work closed as of 2026-05-01. K33 fixed (`c87965f`), K34 documented (`a6a832a`). iPhone Phase 1 smoke closed 2026-04-30 — see Backlog Closed / Superseded.)*

### Andrew-side actions to push Patter to TestFlight (2026-04-23)

The code-side rename is complete and committed. Andrew has these manual/portal steps remaining:

1. **Apple Developer Portal** — register the new App ID `com.andrewporzio.patter` with the iCloud capability enabled (the local build used `-allowProvisioningUpdates` to fetch a profile, but the iCloud container may need to be created/named for the new bundle ID). https://developer.apple.com/account/resources/identifiers/list
2. **App Store Connect** — create a new app record for `com.andrewporzio.patter`, name "Patter", primary language English. The old "AI DJ" record (if one exists) becomes orphaned and can be deleted later. https://appstoreconnect.apple.com/
3. **Archive + upload** — in Xcode, Product → Archive (iOS device destination), then Window → Organizer → Distribute App → App Store Connect. First archive will trigger any remaining provisioning prompts.
4. **Domain registration (optional but cheap)** — `patter.app` and `patter.fm` were both unconfigured during research; grab `patter.app` for ~$15/yr to lock the brand before someone else notices.
5. **USPTO TESS check (optional, before any TM filing)** — manual search at `tmsearch.uspto.gov` for "Patter" in Class 9 (software) and Class 41 (entertainment). Patter LLC exists in lifestyle (rewards/dog-walking) — different class, low conflict risk, but worth eyeballing.
6. **Top-level dir rename (later)** — current working dir is still `~/Library/Mobile Documents/com~apple~CloudDocs/Xcode/AI DJ/`. Renaming to `…/Xcode/Patter/` is a separate mini-task — best done in a clean session because it disrupts Claude Code's active project path and the auto-memory directory path.
7. **GitHub repo rename** — `aporzio1/ai-dj` → `aporzio1/patter`. One click via `gh repo rename patter` (Claude can run this with Andrew's confirmation; not done in the rename commit because rename = destructive on shared state).

### N1 arc — fully shipped (2026-04-30)

The "richer DJ news feature" arc is closed. N1.1 (cross-launch headline dedup) + N1.2 (richer 2–3-sentence briefings using RSS summary, word budget widened to 60–100 for news) had already shipped earlier in the week. **N1.3 (article link on Now Playing) shipped via `ce67651`** — `DJSegment` gained `sourceHeadline: NewsHeadline?`, `Producer.generateSegment` populates it, NowPlayingView renders a "Read article" button via `Environment(\.openURL)` when the current item is a `.news` DJ segment with a non-nil headline. No MiniPlayerBar affordance (per D11). Test constructor sites updated; macOS test suite green.

---

## 4. Backlog

Rough priority, top-down.

| # | Item | Summary | Plan |
|---|------|---------|------|
| 2 | **Album & station detail views** | Station playback works via tap-to-play; album playback not yet verified end-to-end. Dedicated album-detail (track list, shuffle) and station-detail (now-playing only) views. No longer gated on multi-provider work — router abstraction is already in place. Est. medium. | — |
| 3 | **N1 — Richer DJ news feature** | **Closed 2026-04-30.** All three asks shipped: (a) cross-launch dedup, (b) richer 2–3-sentence briefings using RSS summary + 60–100 word budget for news, (c) "Read article" button on Now Playing card during `.news` DJ segments via `ce67651`. Number reserved here pending closed-table promotion in next tracker pass. | — |
| 5 | **K17 — investigate `AttributeGraph: cycle detected`** | Low-priority console warnings observed in earlier smoke runs. Re-run a clean log pass on iPhone and macOS after the Patter rename; if no longer firing, close K17. Est. small. | — |
| 6 | **Path Y — Patter-branded system voice via `AVSpeechSynthesisProviderAudioUnit` extension** | Re-scoped from the original "bundled TTS" plan after K23 found GPL-3 contamination blocks direct linking. Ship a standalone Audio Unit Application Extension that registers a high-quality neural voice (Piper VITS or similar) as a system voice. Extension binary is GPL-3 (clean isolation, links libespeak-ng there); main Patter binary stays MIT/proprietary and talks to the extension via XPC, same pattern as the eSpeak NG iOS app. Voice becomes available to VoiceOver, Reader, Patter's DJ, and any other app — bigger product surface than DJ-only. Est. 5–10 days. **Only legally-clean path to ship Piper on Apple's App Stores.** Two App Store precedents: "Piper - Neural TTS" (id 6759636010) and eSpeak NG iOS. Defer until Path X (shipped 2026-04-26) demonstrates Apple Premium isn't enough on real devices. | (TBD, scope as separate plan doc — must include K22 Constraints & Direction-of-Travel section, see K23 for primary sources) |
| 7 | Kokoro download indicator — real % progress | Replace indeterminate spinner with `ProgressView(value:)`. Blocked on FluidAudio upstream exposing a progress callback. Revisit quarterly. | — |
| 8 | **Sync thumbs feedback to Apple Music ratings** | One-way sync (Patter → Apple Music): when user thumbs-up a track in Patter, also write a love/rating via MusicKit's `MusicLibraryRequest<Track>.ratings`. Reading Apple Music's existing ratings back is out of scope (gated by the same subscription capability and changes there don't push back, so two-way sync would surprise users). Requires `.musicSubscription` capability check + graceful failure for non-subscribers. Confirmed by user 2026-04-30 during smoke (mental model is clearly Apple Music). Smoke unblocker (K26–K29) closed 2026-04-30; ready to schedule. Est. small-medium. | — |
| 9 | **Persona row UX — collapse Duplicate/Duplicate-and-Edit to Edit/Duplicate** | Today's persona-row actions read-only-gate built-ins (`DJPersona.builtIns = [.alex, .chill, .hype, .newsAnchor]`, `isBuiltIn` check) and surface both "Duplicate" and "Duplicate and Edit." User wants the menu collapsed to **Edit** (edits in place, even built-ins) and **Duplicate** (clones-with-edit, same as today's "Duplicate and Edit"). Quote 2026-04-30: "instead of duplicate and duplicate and edit for Personas, it should be Edit, which will edit the one that is there (even if it is built in) and Duplicate (which is the same as duplicate and edit) I don't want it do get cluttered when you already have a persona there to edit." Open sub-decision before coding: **(A) pure in-place edit** of built-ins (lose factory-reset for `.alex` et al — smaller change) vs **(B) implicit fork** where editing a built-in writes a hidden user override that takes the display slot, original definition retained as fallback, "Restore default" affordance added (preserves safety net — bigger change). User wording leans (A); (B) is the safer mental model. Defer A/B pick to the session that picks up the work. Est. small if (A), medium if (B). | — |
| 10 | **AirPlay route picker in MiniPlayerBar + NowPlayingView** | Add a standard AirPlay affordance to both player surfaces — small AirPlay icon that opens the system route picker. iOS path: wrap `AVRoutePickerView` (UIKit) for SwiftUI; macOS path: same shape via the system route control. Should respect both Apple Music tracks (which already route via MusicKit's existing AirPlay support — `ApplicationMusicPlayer` honors the system audio route) and DJ TTS audio (which routes via our `AVAudioSession`/`AVAudioEngine` pipeline — should follow the system route automatically because the session is configured `.playback` with `[]` options, no `.mixWithOthers` / `.duckOthers`, but worth verifying). Possible interaction with K35: if the AVSpeechSynthesizer first-utterance latency is bad on built-in speakers, it may be worse over AirPlay (more re-handshake latency on the route negotiation); resolve K35 first or smoke them together. Est. small if it's just the route picker (~30 LOC + SwiftUI wrapping `AVRoutePickerView`); scope grows if DJ TTS doesn't actually follow the AirPlay route and we need to thread route changes through `AudioGraph`. No open product sub-decisions — fairly mechanical UI add. User asked 2026-05-01: "also put it in the backlog to add an airplay option to the miniplayer and the big player." | — |

### Closed / Superseded

| Original # | Item | Closed | Outcome |
|------------|------|--------|---------|
| 1 (prior) | iPhone Phase 1 smoke | 2026-04-30 | **Verified on Andrew's iPhone 2026-04-30. K25/K26/K27/K28/K29/K31/K32 + N1.3 all confirmed on real hardware.** First pass on 2026-04-30 surfaced K25–K29 plus K31 (re-smoke regression) and K32 (backgrounding paused DJ TTS); fixes shipped same-day in `1c414c8` (K25–K29 bundle), `c9788f0` (K31 scenePhase revert), and `769bbcf` (K32 `UIBackgroundModes`). Andrew's re-smoke after those landed confirmed the full loop is silent-bug-free on iPhone: onboarding, tab bar, MiniPlayerBar transport with shuffle-mode persistence, Library landing, tap-to-play, DJ segments at full volume across foreground transitions and through backgrounding, news commentary with article-link button (N1.3), persona switching, premium-voice nudge copy + deep-link fallback, Kokoro picker disabled on iOS 26. |
| 6 (prior) | Kokoro iOS 26 CoreML compile hang | 2026-04-26 | **Closed by `fd5f397`** — Path X path (c) shipped: iOS 26 auto-downgrades `.kokoro → .system` in `SettingsViewModel`, paired with sentence-chunked premium-voice rendering. Underlying CoreML compile hang is unresolved upstream — see K6 (still open as a watch item). |
| 9 (prior) | Onboarding + iCloud sync — Commit C: Keychain iCloud sync | 2026-04-26 | **Closed by `0bd940b`** — `kSecAttrSynchronizable = true` shipped with a `migrateToSynchronizable` helper invoked from SettingsViewModel on load; OpenAI footer copy updated. |
| 4 (prior) | K5 — remove dead `DJPersona.voicePreset` | 2026-04-30 | **Closed wontfix-with-explanation** per code-review Finding #5 option (a). `voicePreset` is not actually dead — it's a documented fallback used by `Producer.currentVoiceIdentifier` during `primeOpeningIntro()` before `RootView` pushes the first voice. Doc comment added in `Patter/Models/DJPersona.swift`. See K5. |

---

## 5. Open Decisions

| # | Decision | Options | Current recommendation |
|---|----------|---------|------------------------|
| D9 | N1 seen-headline persistence scope | (a) device-local `UserDefaults` only, (b) `CloudSyncService` iCloud-synced across devices | **(a) device-local.** Listening history is per-device behavior; syncing it means a story the DJ talked about on iPhone gets silently skipped on Mac, which the user would experience as "the news never mentions thing X." Re-hearing a story on a second device is the less-surprising default. Revisit if the user asks. |
| D10 | N1 seen-window retention | (a) permanent (once said, never re-said), (b) time-windowed (e.g. 14 days), (c) capped list (e.g. last 200 URLs) | **(c) capped list of 200 URLs, FIFO.** Permanent grows without bound; time-windowed requires per-URL timestamp storage and still lets a story resurface if the user takes a week off. A 200-URL FIFO naturally rolls old stories out as feed content churns, is one `[String]` in UserDefaults, and at ~100 chars/URL is ~20 KB — trivial. |
| D11 | N1 link surfacing location | (a) Now Playing card only (article button visible when DJ news segment is current), (b) Now Playing + MiniPlayerBar compact affordance, (c) persistent "Recent headlines" list in a new tab/sheet | **(a) for commit 3, keep (c) out of scope.** The DJ segment only plays for ~20–30 s — a prominent "Read article" button on the Now Playing card during that window is enough. A persistent history list is a separate feature and should be its own backlog item if the user wants it. MiniPlayerBar is already dense; avoid adding another control. |
| D12 | N1 briefing length impact on TTS budget | (a) keep segment length flat by shortening non-news banter, (b) let news segments run longer (~30 s vs current ~20 s), (c) add a new `DJFrequency`-like knob | **(b) let news segments run longer.** Current prompt caps 30–70 words; a news brief with context wants ~60–100 words. That's a `DJScriptResponse` guide tweak + a second prompt instruction for news, not a new knob. Kokoro renders ~130 wpm so the delta is 5–10 seconds — acceptable for a news beat. Do NOT add a knob; one more frequency slider bloats Settings. |
*(D13 locked + executed 2026-04-26 — see Locked Decisions below.)*

### Locked Decisions

| # | Decision | Locked | Outcome | Rationale |
|---|----------|--------|---------|-----------|
| D17 | Thumbs → Apple Music ratings sync? | 2026-04-30 | **Yes, one-way (Patter → Apple Music) only. Add as Backlog #8, low-medium priority.** | User asked directly during smoke. One-way matches user expectation (their thumbs travel with them) without the surprise of Apple Music ratings silently filtering Patter behavior. Two-way would require reading Apple Music's existing rating store on every track which adds latency + privacy surface. Subscription gate is acceptable — non-subscribers get a no-op write. |
| D16 | Premium-voice nudge — which iOS path to recommend? | 2026-04-30 | **List both: Spoken Content first (primary), VoiceOver → Speech second (alternate). Deep link tries `Content_Speech` → VoiceOver pane → Accessibility root.** | Both paths surface the same voice library because AVSpeechSynthesizer + VoiceOver share the voice store. Listing only Spoken Content occasionally fails because the pane is hidden until the user toggles on Speak Selection / Speak Screen. Listing both adds ~one line of footer copy and removes a real failure mode. |
| D15 | MiniPlayerBar shuffle — one-shot reshuffle or persisted mode toggle? | 2026-04-30 | **Persisted mode toggle, mirroring repeat-mode pattern.** | User's mental model is the native Apple Music shuffle toggle. Repeat is already a persisted mode; symmetry matters. ~30 LOC across `NowPlayingViewModel` + `PlaybackCoordinator` + `MiniPlayerBar`; toggle persists in UserDefaults; `replaceQueue` reshuffles on load when shuffle mode is on. Keep current accent-color treatment for "active" state, matching repeat. |
| D14 | Bug 1 fix — audio session config on iOS | 2026-04-30 | **`setCategory(.playback, mode: .default, options: [])` (drop `.duckOthers`, no `.mixWithOthers`) + scene-active observer in `PatterApp` re-activates session on foreground.** | Patter's product is "hands-off radio listening." When DJ plays, Apple Music is already paused — there is nothing to mix with or duck under. `.duckOthers` is documented for transient prompt audio, not music apps; combined with stale `setActive` it produces the regression. Plain `.playback` is the canonical category for music-playback apps. |
| D13 | Bundled neural TTS — which path? | 2026-04-26 | **Path X (System voice quality + onboarding nudge) shipped 2026-04-26 (`c4206cc` → `24ecdf0`). Path Y (system-voice extension) deferred to Backlog #6 if Path X proves insufficient. Path Z (direct linking) ruled out on K23 GPL-3 contamination.** | See K23. K22 process rule held — license review caught the contamination before any code was written. |
| D1 | Merged library view or segmented provider picker? | 2026-04-22 | **Moot.** Withdrew Spotify; single provider, no picker needed. | Resolved by `ca23170`. |
| D2 | Dev vs prod Spotify Client IDs | 2026-04-22 | **Moot** (Spotify withdrawn). Original: one Client ID. | — |
| D3 | On-device PKCE only vs token-swap server | 2026-04-22 | **Moot** (Spotify withdrawn). Original: on-device PKCE only. | — |
| D4 | macOS Spotify path | 2026-04-22 | **Moot** (Spotify withdrawn). Original: ship iOS-only alert, defer WKWebView spike. Cross-ref K21. | — |
| D5 | Update `CLAUDE.md` re: simulator unusability | 2026-04-22 | **Dropped.** No longer relevant without Spotify. | — |
| D6 | macOS Spotify behavior in Phase 2b | 2026-04-22 | **Moot** (Spotify withdrawn). | — |
| D7 | Scope of Phase 2b polish commit | 2026-04-22 | **Moot** (Spotify withdrawn). Constituent items absorbed into backlog (K5, K17, Library empty state). | — |
| D8 | Spotify playlist-tracks 403 + decoder malformedResponse | 2026-04-22 | **Moot** (Spotify withdrawn). Originally recommended hybrid D8(a) migration to `/playlists/{id}/items`. Never executed. | — |

---

## 6. Known Issues / Tech Debt

| # | Title | Severity | Note |
|---|-------|----------|------|
| K1 | `FakeAudioGraph` main-actor conformance | resolved | Closed in `c325b56`. Stacked: Info.plist, main-actor, stale assertions. |
| K2 | `@preconcurrency import FluidAudio` | low | FluidAudio isn't Swift 6 strict-concurrency-clean. Revisit on upstream update. |
| K3 | No per-voice mood descriptors for Kokoro | low | Upstream doesn't publish them. |
| K4 | App-bundle Spotify Client ID would leak | closed | Closed — Spotify withdrawn (K21). |
| K5 | `DJPersona.voicePreset` is documented fallback (not dead) | **closed (wontfix-with-explanation) 2026-04-30** | **Reframed and closed 2026-04-30 per option (a) on code-review Finding #5 (`a189fe590578e8f07`).** Original framing called `voicePreset` "dead weight" because `SettingsViewModel.effectiveVoiceIdentifier` ignores it when the user has picked a voice. Code review revealed this was wrong: `Producer.currentVoiceIdentifier` (Producer.swift:116) uses `voicePreset` as a fallback when `voiceOverride` is nil — which happens during `primeOpeningIntro()` *before* `RootView` pushes the first voice. Removing the field would force one of two worse outcomes: (1) hardcode a voice ID into Producer (voice-routing logic leaking into a scheduler), or (2) thread the persona's voice through Producer init (over-engineered for a one-call-site fallback). Documented with a doc comment in `Patter/Models/DJPersona.swift` explaining the fallback role. Backlog #4 closes with same disposition. |
| K6 | `.help(...)` used as iOS accessibility hint | **closed 2026-04-30** | **Closed via `b04dde8` (code-review Finding #8).** Grep across the source tree turned up a single hit at `NowPlayingView.swift:166` (Regenerate DJ button); fixed by wrapping in `#if os(macOS)`. Grep is now clean. Standing rule retained for future code: `.help(...)` is macOS-only; iOS VoiceOver needs `.accessibilityLabel`. Re-grep on any new modifier sprawl. |
| K7 | `RSSFetcher.fetchHeadlines().first` always picks top | **superseded 2026-04-21 by N1** | In-memory rotation landed (see `Producer.recentHeadlineURLs`, cap 10) but does not survive app restart, and the broader "richer commentary + user-visible link" ask extends past pure rotation. Tracked as Backlog #3 (N1). |
| K8 | `voiceIdentifier` is device-local, but iCloud-synced | low | AVSpeech voice identifiers are per-device-install. Silent fallback to persona preset on missing. |
| K9 | FluidAudio exposes no download-progress callback | low | See Backlog #7. |
| K10 | `xcodegen generate` was stripping entitlements | resolved | Fixed in `dd6ff07`. New entitlements must be declared in `project.yml` to survive regen. |
| K11 | Empty-cache poison pattern | resolved | Fixed in `5c09db9`. |
| K12 | `MusicProviderService` leaked `MusicKit.Artwork` | resolved | Fixed in `8bb13d3`. |
| K13 | Build-system errors mask logic errors | low | Any time a test target goes red after a long quiet period, check `project.yml` target settings before debugging Swift. |
| K14 | Test suite staleness after refactor waves | low | Anytime a protocol-level contract shifts, sweep `PatterTests/` in the same commit. |
| K15 | Transport transitions must bump `playbackGeneration` + stop `audioGraph` | med | Two invariants in `PlaybackCoordinator`: (1) end-of-segment transitions must `audioGraph.stop()` + bump generation; (2) transitions into `.playing` must set state before `monitorTrackUntilEnd`. Any future transport work must respect both. |
| K16 | macOS 26 `ASWebAuthenticationSession` libdispatch crash | closed | Closed — Spotify withdrawn (K21). General note retained: on macOS 26, `NSWorkspace.open` + `.onOpenURL` is a viable workaround if another feature hits a similar framework crash. |
| K17 | `AttributeGraph: cycle detected` console warnings | low | Observed in earlier smoke runs; verify on a fresh log pass after the Patter rename and close if no longer firing. See Backlog #5. |
| K18 | Spotify Web API Feb-2026 migration broke DTOs | closed | Closed — Spotify withdrawn (K21). |
| K19 | Decoder tolerance is the wrong tool for a field rename | med | **Kept — general lesson.** When a 200 response produces `malformedResponse`, dump the raw body first. Tolerance layers (`try?`-decode, optional fields) hide renamed parent keys and turn populated responses into silent nils. Applies to any JSON-decoding provider we add in the future. |
| K20 | Vendor APIs churn; annotate endpoints with last-verified date | low | **Kept — general lesson.** Annotate every external-API call site with `// Verified against <vendor> docs YYYY-MM-DD`. First place to look on a 4xx. Applies to OpenAI TTS, any future music-provider REST, any cloud endpoint. **First annotation landed on `OpenAIDJVoice.swift:75` on 2026-04-30** for the `audio/speech` endpoint + body fields (per code-review Finding #11). Other external API call sites still need a sweep. |
| K21 | Spotify iOS has no standalone-playback path for third-party apps | **locked (high)** | **Verdict locked 2026-04-21.** No first-party or sanctioned SDK lets a third-party iOS app on the App Store stream Spotify audio in-process. Four pillars: (1) SPTAppRemote is IPC to the Spotify app, not a streaming SDK; (2) Web Playback SDK is browser JS + EME DRM, unsupported in `WKWebView`; (3) Spotify Connect Partner Program is hardware-OEM licensing only; (4) Developer Terms §III.2 prohibits streaming/proxying raw audio — djay lost Spotify July 2020 when the partnership was revoked; djay/Serato DJ/Traktor DJ have all shipped without Spotify since. Direction of travel is tighter, not looser. **Re-opening requires Andrew to produce a named App Store app that plays Spotify without the Spotify app installed** — unnamed rumor doesn't reopen this. 80%+ probability any candidate is SPTAppRemote; remaining 20% checkable in minutes by running the candidate without the Spotify app on-device. Closed out active Spotify work; drove `ca23170` (Option B — rip). |
| K22 | Do vendor-constraint research before starting SDK integration, not after | **high (process)** | **New 2026-04-22.** The Spotify arc cost ~two working days — 11 integration commits (`d69eadc` through `10fc7f3`), 7 smoke-fix commits (`8d926fd` through `3c968fc`), a Feb-2026 API-migration pivot (K18), and ultimately a full rip (`ca23170`). K21 research, which closed the question decisively, could have been run in ~30 minutes before starting Phase 2a and would have prevented the entire arc. **Standing rule going forward:** before starting any vendor-SDK integration (music providers, TTS providers, ASR providers, cloud LLMs with novel auth, any closed-source iOS SDK), the plan doc must include a **Constraints & Direction-of-Travel** section citing primary sources on: (a) what the SDK actually does vs what it appears to do (IPC vs streaming, remote-control vs playback, etc.), (b) vendor ToS restrictions on the intended use, (c) historical precedents — named apps that tried this and what happened, (d) direction of travel over the last 24 months (tightening or loosening). PM agent should flag missing Constraints section as **Hold** on any new SDK plan. Cost to enforce: ~30 min of research per SDK. Cost of skipping: potentially ~2 days per SDK. Applies retroactively — future SDK plans must contain this section. **K22 has now paid for itself once** — see K23 (TTS spike) where it caught the GPL-3 contamination before any code was written. |
| K24 | iOS 26 Kokoro CoreML compile hang (underlying issue) | med | **Surfaced 2026-04-26.** Backlog #6 closed via `fd5f397` path (c) — iOS 26 fresh installs auto-downgrade `.kokoro → .system` in `SettingsViewModel`, so users don't experience the hang. The underlying FluidAudio/CoreML compile hang on iOS 26 is unresolved upstream. If a user manually flips back to Kokoro on iOS 26, they will still hit it. Revisit if FluidAudio publishes an iOS 26 compatibility note, or if users complain about being gated to System voice on iPhone. |
| K25 | Audio volume drops every time Patter foregrounds | **closed 2026-04-30** | **New 2026-04-30 from iPhone smoke; closed same day.** Original diagnosis: `AudioGraph.swift:14-18` called `setCategory(.playback, options: [.duckOthers])` then `setActive(true)` only in `init()`; theory was that `.duckOthers` plus no `setActive` on `scenePhase → .active` left the session ducked. **Initial fix bundled two changes:** (a) drop `.duckOthers` and (b) add a scenePhase observer in `RootView` that called `audioGraph.reactivate()` on `.active`. **Outcome:** the `.duckOthers` removal alone fixed the user-visible bug; the scenePhase observer was speculative scope creep and introduced K31 (iOS MusicKit playback breakage on iPhone). **Reverted 2026-04-30:** scenePhase observer + `reactivate()` plumbing removed; `init()` configures the AVAudioSession exactly once. Andrew's re-smoke confirmed volume still holds across foreground transitions, so the user-visible bug stays fixed. See K31 for the full revert + lesson. |
| K26 | Kokoro silently breaks DJ on iOS 26 if user re-picks after auto-downgrade sentinel burns | **closed (verified on iPhone) 2026-04-30** | **New 2026-04-30 from iPhone smoke; closed same day.** `SettingsViewModel.swift:226-240` `applyIOS26KokoroDowngradeIfNeeded()` is one-shot per device (sentinel `kokoroAutoDowngradedFromIOS26`). After the sentinel burns, user could re-pick Kokoro in Settings; on next DJ load CoreML hits the `libBNNS.dylib` SME2 segfault and DJ "just skips" with no surfaced error. Existing in-app `try/catch` in DJVoiceRouter cannot catch a process-level segfault — comment in code at line 220-222 acknowledges this. **Fix shipped via `1c414c8`:** hard-disable `.kokoro` in the picker on iOS 26 (gray out + footer "Kokoro requires iOS 25 or earlier — CoreML compatibility issue"). Same intent as the auto-downgrade but enforced at the UI affordance level, removing the silent-breakage path entirely. Closes the gap that K24's "user can manually flip back" caveat acknowledged. **Verified on Andrew's iPhone 2026-04-30** — picker disable behaves as expected; no silent DJ skip on iOS 26. |
| K27 | MiniPlayerBar shuffle button has no visible state and feels broken | **closed (verified on iPhone) 2026-04-30** | **New 2026-04-30 from iPhone smoke; closed same day.** `MiniPlayerBar.swift:138-150` foreground style was hard-coded `.secondary` with no toggle state; `vm.shuffleUpcoming()` fired and `PlaybackCoordinator.swift:225-230` reshuffled the queue tail correctly, but the user saw nothing. **Fix shipped via `1c414c8` per D15:** converted from one-shot reshuffle to persisted mode toggle (matches Apple Music + repeat-mode pattern). ~30 LOC across `NowPlayingViewModel` + `PlaybackCoordinator` + `MiniPlayerBar`. **Verified on Andrew's iPhone 2026-04-30** — shuffle now toggles visibly and persists across launches. |
| K28 | Playlist detail Play button: invisible icon + off-center text | **closed (verified on iPhone) 2026-04-30** | **New 2026-04-30 from iPhone smoke; closed same day.** `PlaylistDetailView.swift:42-58`. Both Play and Shuffle used `Label(...)` + `.frame(maxWidth: .infinity, minHeight: 44)`. Shuffle rendered correctly; Play's `play.fill` symbol was invisible (likely `.borderedProminent` foreground/tint interaction on iOS 26) and the inner HStack was left-aligned. **Fix shipped via `1c414c8`:** replaced `Label` with explicit `HStack { Image; Text }`, dropped the inner `maxWidth: .infinity`, let the button's outer frame fill width. **Verified on Andrew's iPhone 2026-04-30** — Play button icon visible and label centered. |
| K29 | Premium-voice nudge text references only Spoken Content path | **closed (verified on iPhone) 2026-04-30** | **New 2026-04-30 from iPhone smoke; closed same day.** `SettingsView.swift:230,432,442,448` and `PreferencesWizardView.swift:194` originally recommended Settings → Accessibility → Spoken Content → Voices only. The VoiceOver → Speech path surfaces the same voices via the shared voice store and is sometimes more discoverable (Spoken Content can be hidden until user toggles Speak Selection / Speak Screen). **Fix shipped via `1c414c8` per D16:** lists both paths, deep-link tries `Content_Speech` then VoiceOver pane then Accessibility root. **Verified on Andrew's iPhone 2026-04-30** — copy reads cleanly and deep-link fallback chain works. |
| K30 | macOS app-sandbox / MusicKit IPC — **spurious diagnosis** | low (kept visible for breadcrumb) | **Filed 2026-04-30, reclassified same day after K31 disambiguation.** The original symptom set (`applicationQueuePlayer _establishConnectionIfNeeded timeout`, `ICError -7013`, ACAccountStore Code=9, `xpc service connection interrupted`) was misattributed to macOS Seatbelt enforcement of `com.apple.security.app-sandbox`. Andrew was on **iPhone**, not macOS — the container path `/var/mobile/Containers/...` in his log is iOS, full stop. The real cause is K31 (scenePhase observer racing `ApplicationMusicPlayer`'s IPC handshake). **The `app-sandbox` rollback in `885e825` is harmless and stays** — on iOS the entitlement key is a no-op (iOS sandboxing is OS-enforced via code-signing, not this key), so disabling it costs nothing today. Re-enabling it before Mac App Store submission will require the mach-lookup exception audit originally described in this entry, but that is future work, not active. **Lesson — container path is platform ground truth:** when a log shows `/var/mobile/Containers/...` it is iOS; when it shows `~/Library/Containers/...` it is macOS. Confirm the device before reasoning about platform-specific subsystems. The original entry mistook a Spotlight-style symptom catalogue for the right diagnosis without checking the path prefix. |
| K31 | iOS MusicKit playback breaks after first DJ segment (scenePhase reactivation race) | **closed 2026-04-30** | **New 2026-04-30, closed same day.** K25's initial fix added an `.onChange(of: scenePhase)` block in `RootView` that called `audioGraph.reactivate()` on `.active`. `reactivate()` invoked `AVAudioSession.setActive(true)` a second time, shortly after `AudioGraph.init()` already did the same. On iOS 26 that second activation races `ApplicationMusicPlayer`'s IPC handshake at app launch and breaks track playback after the first DJ segment with `applicationController: xpc service connection interrupted` → `prepareToPlay failed [no target descriptor]`. Andrew's iPhone re-smoke after disabling the scenePhase observer confirmed: tracks play after DJ intro (no IPC interruption sequence), AND volume still holds across foreground transitions (so K25's user-visible bug stays fixed by the `.duckOthers` removal alone). **Fix:** scenePhase observer + `@Environment(\.scenePhase)` declaration removed from `RootView.swift`; `nonisolated reactivate()` and `private nonisolated configureSession()` removed from `AudioGraph.swift`; iOS session setup inlined back into `init()` as a single call site. Net change: smaller surface than what K25 originally shipped. **Process lesson — surface-area discipline on bug fixes.** K25 originally bundled two changes (drop `.duckOthers` + add scenePhase reactivation) for one user complaint (foreground volume drop). Only the first was load-bearing; the second was speculative belt-and-suspenders that introduced this regression. **Standing rule going forward:** when triaging a single user-visible bug, lean toward the smallest-possible-change fix. Belt-and-suspenders additions (a second `setActive`, a redundant retry, a defensive notification observer) are only justified when there's evidence the primary fix is insufficient — not on speculation that something might also be a problem. The PM consultation should ask "is this fix one change or two?" and push back on bundles unless the bundle has named evidence. Cost of skipping: ~one user-blocking regression (this one) plus the debugging cycle to disambiguate it from K30. Cost of enforcing: a few minutes of consultation pressure-testing. |
| K32 | DJ TTS audio paused on app background; only Apple Music kept playing | **closed (verified on iPhone) 2026-04-30** | **New 2026-04-30 from iPhone re-smoke; closed same day (`769bbcf`); re-verified on iPhone after `769bbcf` landed.** When Andrew backgrounded Patter mid-DJ-segment, the DJ paused and resumed only when the app returned to foreground. Apple Music tracks were unaffected. **Cause:** `UIBackgroundModes` was missing from `Patter/Resources/Info.plist` / `project.yml`. Apple Music tracks survive backgrounding because `ApplicationMusicPlayer` renders out-of-process via the system `musicd` daemon — the host app being suspended doesn't stop them. DJ TTS goes through AVAudioEngine in the Patter process; without `UIBackgroundModes` declaring `audio`, iOS suspends the process (and thus the engine) on background. **Fix:** added `UIBackgroundModes: [audio]` to `project.yml`'s `info.properties` block; `xcodegen generate` propagated to `Patter/Resources/Info.plist`. macOS test suite still green at 32. **Lesson — audio session category and `UIBackgroundModes` are a pair, not separate concerns.** This was a long-standing gap (the entitlement was never declared from day one) that only became user-visible once Andrew started actually backgrounding the app mid-segment during smoke. Earlier sessions configured `AVAudioSession.setCategory(.playback, ...)` without ever asking "does the host process also need to stay alive?" The two concerns answer different questions — category says "what kind of audio is this" and `UIBackgroundModes` says "is the OS allowed to suspend us while it's playing." Both must be declared for in-process audio to survive backgrounding. **Standing rule going forward:** any new app or any new audio pipeline added to an existing app should set the audio background mode at the same time the audio session category is set; treat them as a checklist pair. **Side note on `Info.plist` versioning:** `CFBundleVersion` auto-bumps on every `xcodegen generate` (this commit captured 142 → 157). Going forward we let those bumps land in commits rather than excluding them — they're App Store build numbering, not real working-tree drift. |
| K33 | `PlaybackCoordinator.monitorTrackUntilEnd` bumps `playbackGeneration` at the wrong layer | **closed 2026-05-01** | **New 2026-04-30 from code review (`a189fe590578e8f07`); closed 2026-05-01 by `c87965f`.** Original problem: `monitorTrackUntilEnd` bumped `playbackGeneration` *inside* the monitor, so two near-simultaneous play-initiation calls could each capture the same `myGen` and run concurrently, double-advancing the queue at end-of-track. **Fix shipped:** extracted `private func startNewGeneration()`; three play-initiation sites (`playTrack`, resume-from-pause in `play()`, `playSegment`) bump *before* invoking the monitor; `monitorTrackUntilEnd` now captures the value its caller already set. Five transport-interruption sites (`replaceQueue`, `startStation`, skip, previous, `swapCurrentSegment`) keep their inline `playbackGeneration += 1` per Andrew's guidance — those are interruption sites, not initiation sites, and the inline bump is the more legible local idiom there. `playbackGeneration` is now `private(set)` for test-target observation. New `playbackGenerationContract` test brings macOS suite to 35 (was 34). **Lesson — every play-initiation site bumps; monitor no longer self-fences.** New K15-style invariant: any new play-initiation call site must call `startNewGeneration()` before invoking the monitor; reviewing the monitor's body is no longer enough — the audit has to walk every initiation call site. |
| K34 | `AudioGraph.stop()` deferred-Task continuation race | **closed 2026-05-01 (documented, residual race accepted)** | **New 2026-04-30 from code review (`a189fe590578e8f07`); closed 2026-05-01 docs-only by `a6a832a`.** Reviewer's original suggested fix — "capture `pendingContinuation` at stop-call time, before the Task spawn" — turned out to be impossible: `stop()` is sync because it's driven by `withTaskCancellationHandler.onCancel`, which doesn't permit async, so any actor-state read happens *inside* the spawned Task — which is the same race the suggestion was trying to close. **Documented residual race window:** at most a too-early resume of a new `play()` (~50 ms audible skip), not data corruption. `resumePending()`'s atomic read+nil ensures no double-resume. Race requires user-tap-pause + new-play in the same actor turn — not a natural user flow. Comment block added above the `nonisolated(unsafe)` annotations explaining the suppression and standing rule for future AudioGraph touches to re-validate the three preconditions. A real fix would require either (i) async `stop()` with large blast radius or (ii) per-`play()` UUID-keyed continuation slots (contract redesign); both rejected for now, revisit if observed in production logs. **Lesson — when a reviewer suggests a fix, validate the actor-isolation shape before locking the plan: `nonisolated` synchronous functions can't atomically capture actor state.** Treat reviewer-proposed fixes the same as one's own — pressure-test the call-site shape before committing to the plan. Race is documented, not eliminated; revisit if observed in logs. |
| K35 | DJ TTS System-voice AU cost — two symptoms, one root cause, three fix layers | **closed 2026-07-05 — device-verified on Andrew's physical iPhone (was high)** | **Filed 2026-05-01 (`2f48065`). Diagnosis text corrected 2026-07-05 — the original entry described code (a fresh `AVSpeechSynthesizer` per render call) that had already been replaced by `9779040` the same day it was filed; that fix, and a second same-day fix (`d97ce8e`), were never logged in this tracker until now. Root cause is one thing — `AVSpeechSynthesizer`'s Audio Unit IPC handshake and per-render AU activation touching the shared `AVAudioSession` — presenting as two distinct user-visible symptoms.** **Symptom A (first logged 2026-05-01):** first DJ utterance after a new script renders has an audible ~1–2s delay before audio starts; later utterances are tighter. **Symptom B (surfaced during the spike, same day):** when Producer's next-up DJ render kicks off ~30s before track end, the AU handshake/activation transiently disrupts the shared session, which on iOS pre-empts `ApplicationMusicPlayer` and freezes the *currently-playing track* for ~10s. **Fix layers, all shipped in code:** (1) `9779040` (spike F, 2026-05-01) — hoisted one long-lived `AVSpeechSynthesizer` onto `SystemDJVoice`/`SpeechRenderer` so renders 2+ skip the handshake. **iPhone re-test showed this alone was insufficient** — track playback still stalled at `willAdvance`. (2) `d97ce8e` (spike J, 2026-05-01, same day) — refined diagnosis: the load-bearing cost wasn't the handshake but per-render AU *activation* touching the shared session on every `write()` call regardless of synth lifetime; set `usesApplicationAudioSession = false` on the long-lived synth (iOS-only) so it gets a private AU session — costs nothing audible since rendering is in-memory PCM, not through-speaker. Targets symptom B. (3) `bc9a871` + `8794495` (2026-07-05) — added a `DJVoiceProtocol.warmUp(voiceIdentifier:)` hook, implemented on `SystemDJVoice` as a throwaway launch-time render, serialized via a task chain (race closed by `8794495`) so it can't collide with a real Producer render; wired into `RootView.handleReady` mirroring the existing DJBrain/Kokoro warm-up. Targets symptom A. **Device verification (2026-07-05, physical iPhone, iOS 27, `9f591b7` timing diagnostics):** both original gate criteria now satisfied. Symptom A — `SystemDJVoice.renderToFile` (the real, user-facing render) measured **0.81s**, sub-second, confirming the AU-handshake cost is paid during the invisible launch warm-up window instead of the live DJ segment. Symptom B — MusicKit poll counters ticked cleanly throughout the run with no `xpc service connection interrupted` / `prepareToPlay failed`, the K31/K35 failure signature is absent. The launch warm-up itself measured 6.57s, elevated but expected/by-design (runs concurrently with DJBrain's own ~11-12s Foundation-Models warm-up under `.utility` priority, contending for launch-time CPU/AU resources) — invisible to the user, not a regression. **Both gate criteria closed — K35 fixed and device-verified.** Note: Andrew's subjective post-fix read ("I don't think it was quicker") is real but is explained by a *different*, previously-unmeasured bottleneck (DJBrain LLM generation cost, not TTS/AU-handshake cost) — see K38, filed separately, does not reopen K35. Compounds with K32-style backgrounding (engine resumes from suspend → cold-start risk again) and may interact with Backlog #10 (AirPlay route negotiation) — both remain live watch items even though K35 itself is closed. **Lesson — log fixes the day they ship, not the day the bug closes.** Two real fixes (`9779040`, `d97ce8e`) sat unlogged in git for over two months because the tracker-update step was skipped after a docs-only commit (`2f48065`) that filed the bug but predated the same-day code fixes. Standing rule: any session that ships a code fix for an open Known Issue updates that issue's row in the same tracker pass, even if verification is still pending. |
| K36 | `RSSFetcher` cross-feed dedup winner is nondeterministic under concurrency | low | **New 2026-07-05 (`60787ed`).** Fanning out per-feed fetches via `withTaskGroup` means task completion order isn't guaranteed, so when the same URL appears in two feeds, which feed's copy survives dedup is no longer deterministic. Accepted — duplicates are expected to be near-identical, and tests assert dedup count only. **Tripwire:** don't write a future test asserting *which* title/feed wins on a duplicate URL — it will be flaky by design. |
| K37 | iOS 26 Kokoro CoreML crash guard didn't cover iOS 27 (K6/K24 follow-up) | **closed 2026-07-05 — device-verified (`aaec296`)** | **New 2026-07-05, found and fixed same session on Andrew's physical iPhone.** Device was running **iOS 27** with `ttsProvider` persisted as `.kokoro` from before an OS upgrade. Both existing guards — `SettingsViewModel.applyIOS26KokoroDowngradeIfNeeded()` and `TTSProvider.available` in `DJVoiceRouter.swift` — checked `ProcessInfo.processInfo.operatingSystemVersion.majorVersion == 26` (exact match), so neither fired on 27. Kokoro's CoreML compile hard-failed with a **different** crash signature than the original K6/K24 bug — an MPSGraph/MLIR assertion ("axes in perm tensor must be unique") vs. the original libBNNS SME2 segfault — and hung the app ("stays loading and locked up"). Same underlying problem recurring on a newer OS: Kokoro's CoreML graph doesn't survive Apple's on-device ML stack, cross-ref K6/K24. Because `RootView.handleReady` auto-warms Kokoro at every launch whenever `.kokoro` is persisted, this would have re-hung on every relaunch — reinstalling the old build would not have helped, only a code fix does. **Fix (`aaec296`):** widened both guards from `== 26` to `>= 26` — no evidence a future major version fixes this without a FluidAudio update, and betting on version-specific detection isn't safe given the crash signature already differs between 26 and 27. **Device-verified:** rebuilt, reinstalled, relaunched on the same iOS 27 device — guard fired correctly, no Kokoro compile attempt anywhere in the log, provider auto-downgraded to `.system`, `.caf` renders throughout, no hang. **Lesson — exact-match OS-version guards are fragile across upgrades.** A capability/compatibility gate written as `== N` silently stops protecting the user the day they upgrade past N; write these as `>= N` (or feature-detect the actual capability) unless there's a specific reason a *later* OS is known-safe. |
| K38 | DJBrain Foundation Models generation latency (4-6s) is the dominant remaining DJ-segment startup cost | **open — no investigation, deferred** | **New 2026-07-05, observed during the same device smoke that closed K35.** With K35's render slice now sub-second (0.81s), Andrew's subjective read after rebuilding was still "I don't think it was quicker" — explained by a different, larger, previously-unmeasured bottleneck. On the same run: `DJBrain.generateScript` (the on-device Foundation Models LLM call that writes the DJ script) took **4.3s**. `DJBrain.warmUp()` (pre-existing, unrelated to K35, already in the codebase before this session) spent **~11-12s** at launch but the real generation call still paid close to full cost — the warm-up doesn't appear to meaningfully reduce it. Total dead-air from DJ-segment-start to audio-ready is roughly generation (4.3s) + render (0.81s) ≈ **5.1s**. K35 only ever targeted the render slice (the smaller one, now fixed); the generation slice is bigger and almost certainly the dominant remaining contributor to the dead-air Andrew still perceives. **This is a genuinely different bottleneck than K35** — LLM inference cost, not TTS/AU-handshake cost — and needs its own investigation. No diagnosis or fix attempted: Andrew explicitly chose to close K35 and defer this to a future session rather than dig in now. Context, not diagnosis: `SystemDJVoice.warmUp` and `DJBrain.warmUp()` run concurrently at launch under `.utility` priority and may be contending for the same CPU/AU resources (see K35's 6.57s warm-up note), which could be *part* of why DJBrain's own warm-up isn't paying off — unconfirmed, flagged for whoever picks this up. |
| K23 | Bundled neural TTS via sherpa-onnx + Piper: GPL-3 contamination blocks direct linking; system-voice extension is the only clean App Store path | **locked (high)** | **New 2026-04-26 from K22 spike on the bundled-TTS proposal. Path X shipped 2026-04-26 (`c4206cc` → `24ecdf0`); Path Y deferred to Backlog #6.** Original plan: vendor `sherpa-onnx` (Apache-2.0, C++ TTS runtime) + a Piper VITS English voice (~50–100 MB) into the main Patter binary as a fourth `DJVoiceRouter` provider, ship offline + free + on-device. **Verdict: Hold the direct-link path; only the `AVSpeechSynthesisProviderAudioUnit` extension pattern is legally clean for App Store distribution.** Findings, by K22 criteria: **(a) what the SDK actually does** — sherpa-onnx v1.12.40 (released 2026-04-24) is real, actively maintained, supports iOS arm64 + Simulator + macOS arm64, has a Swift sample. Apache-2.0. But it does NOT publish prebuilt xcframeworks; build-from-source is required for Apple platforms (multi-day build-system work). For Piper VITS voices, sherpa-onnx **statically links espeak-ng during its build AND requires the `espeak-ng-data/` directory at runtime** for phoneme conversion. Per Piper maintainers: "Piper outsources text-to-phoneme conversion to espeak-ng (for now)." No production-ready Piper voices skip espeak. **(b) ToS / license restrictions — the fatal finding.** espeak-ng is **GPL-3.** If Patter (closed-source App Store app) statically links sherpa-onnx-with-espeak-ng into the main binary, GPL-3 propagates to Patter itself, conflicting with both the iOS App Store and Mac App Store. FSF position unchanged in 2026: GPL apps cannot ship via Apple's App Stores due to Developer Program License Agreement anti-redistribution clauses conflicting with GPL-3's anti-additional-restrictions clause. The clean way: put GPL-licensed library inside an Audio Unit Application Extension (`AVSpeechSynthesisProviderAudioUnit`), license the extension as GPL-3, keep main app GPL-free via XPC. Voice licenses are a second audit — `rhasspy/piper-voices` repo top-level is MIT but per-voice licenses (CC-BY-4.0, MIT, etc.) must be checked individually with attribution. **(c) historical precedents** — "Piper - Neural TTS" by IHOR SHEVCHUK (App Store id 6759636010, iOS 18+, 87.7 MB, no IAP, no data collection): existence proof Piper voices ship on App Store, but only via the extension pattern. eSpeak NG iOS app: same Audio Unit Extension pattern; `LICENSE.md` explicit (Extension = GPL-3, Application = MIT). VLC iOS (2011): canonical "got pulled from App Store for GPL conflict" — FSF still cites it. **(d) direction of travel** — **Tightening** on GPL: no FSF position change since 2010; 2026 Apple guidelines emphasize "self-contained bundles, no third-party installers, updates solely via the Mac App Store." **Loosening** on on-device TTS: iOS 17 introduced Premium voices as on-device + free + neural; iOS 18+ Apple Intelligence shipped more on-device language tech; the "Apple Premium" vs "Piper medium" gap has narrowed materially. The "robotic Compact voices on fresh install" problem the original plan tried to solve is increasingly a "prompt the user to download a Premium voice" UX problem rather than a "ship our own neural model" engineering problem. **Outcome:** original plan moved to **Hold**; Path X (System voice quality + onboarding nudge) recommended as next move (see §3 + D13); Path Y (system-voice extension) remains as Backlog #7 if Path X proves insufficient; Path Z (direct linking) ruled out. **Lesson — what we'd do differently:** the K22 Constraints section should have been written as the first action of the spike, not the last. The license question (criterion b) was always the load-bearing one and could have been resolved in 10 minutes by reading sherpa-onnx's CMake and Piper's README. Going forward, when a K22 spike is greenlit, criterion (b) ToS/license review must be the first thing checked — if it fails, the rest of the spike is moot. |

---

## 7. Plans & Specs

| Doc | Status | Summary |
|-----|--------|---------|
| `docs/superpowers/specs/2026-04-17-ai-dj-design.md` | **archived 2026-07-05 (`c8d48a9`)** | Original architecture spec for the AI DJ MVP. Marked archived with an explicit stale-content callout (naming, Swift version, scope predate Spotify removal / personas / CloudSync / RSS / TrackFeedbackStore). Retained for historical background per `CLAUDE.md`; `docs/project-tracker.md` is the live-state source. |
| `docs/superpowers/plans/2026-04-20-spotify-support.md` | **WITHDRAWN 2026-04-22 per K21** | Phased implementation plan for adding Spotify as a second music provider. Not deleted — useful historical context for (a) the phased-refactor pattern (Phase 1a/1b/1c + router abstraction shipped successfully and remain in the codebase), (b) the cost of skipping direct-source research (K22), (c) reference if a different music provider is considered in the future. Top-of-doc note added flagging withdrawal. Cross-ref: K21, K22, `ca23170`. |
| `docs/superpowers/plans/2026-07-05-codebase-improvement-pass.md` | **active — Phase 0/1/2 shipped and device-verified, Phase 3/4 not started** | Multi-phase refactor + testability pass on branch `refactor/codebase-improvement-pass`. Phase 0: 9 zero-behavior-change quick wins (shipped). Phase 1: K35 SystemDJVoice launch warm-up — **shipped and fully closed**, device-verified on Andrew's physical iPhone (0.81s render, no MusicKit disruption); same smoke also fixed an unrelated iOS-27 Kokoro guard gap (K37, closed) and surfaced a new, larger, deferred bottleneck in DJBrain generation latency (K38, open). Phase 2: testability + coverage, all 7 items shipped, 35→106 tests (exceeded the ~60 estimate). **Note: the plan's "Execution-environment caveat" (Linux container, no Xcode, "tests green" = unverified structure) was stale for the session that actually did this work** — that session ran on Andrew's Mac with real Xcode access and verified every Phase 0/1/2 commit via actual `xcodebuild test`/`build` runs; see §2's 2026-07-05 entries for detail. Phase 3 (Settings restructure) and Phase 4 (macOS sandbox spike) not started — both gated on human-perceptual verification, not automatable regardless of Xcode access. |

---

## 8. Withdrawn: Spotify Integration Arc (2026-04-20 – 2026-04-22)

**Status:** Ripped out in `ca23170`. Retained here as an index of commits that shipped to `main` and were subsequently withdrawn, so future readers can trace history without scanning git.

**Withdrawal rationale:** K21 (locked) — no first-party iOS SDK path exists for a third-party App Store app to stream Spotify audio standalone. The SPTAppRemote path requires the user to have the Spotify app installed and running, which breaks the "hands-off radio" product goal. Pivoted to Apple-Music-only rather than ship a compromised experience.

**Phase 1 (refactor) — retained, still in the codebase:**
- `79df4af` — Phase 1a: rename to `MusicProviderService`, provider-namespaced `PlayableItem.id`
- `8bb13d3` — Phase 1b: provider-neutral `ProviderArtwork`
- `3d09898` — Phase 1c: `MusicProviderRouter`

These are the "paid-for" part of the arc. They're valuable abstraction even with one provider and cost nothing to keep.

**Phase 2a (read-only Spotify) — all withdrawn:**
- `d69eadc`, `9dd5d39`, `c9b0583`, `ac8a5ed`, `8a1eeee`, `10fc7f3`

**Phase 2a smoke fixes — all withdrawn:**
- `8d926fd`, `51b1080`, `a2c1516`, `564037f`, `4b5e97b`, `2854cd6`, `3c968fc`

**Phase 2b (SDK playback) — partially shipped before rip, all withdrawn:**
- `6497e5f` (2b.1 — SPM package), `aea38c3` (2b.2 — SPTAppRemote), `d57b3f9` (provider-scope library cache), `e62874c` (playlist diagnostics), `f2d6dbb` (human errors), `0c7de2d` (OAuth scope logging), `4f5381b` (scope persistence), `00c9bb0` (/me user id log), `ed6cdcf` (access probe matrix), `87e5180` (playlist-tracks pivot), `f023332` (tolerant track decoding), `b81a5ba` (raw body logging), `79ce718` (Feb-2026 API migration), `988faced* not applicable, `b1ea2ed` (persist toggles — **retained, unrelated**), `7fe11a6` (SPTAppRemote open-Spotify failure), `baed2f0` (SPTSessionManager 2b.3).

**Rip commit:**
- `ca23170` — refactor: drop Spotify integration, Apple Music only (Option B). 2,141 lines deleted.

**Lessons captured:** K21 (constraint), K22 (process rule), K18/K19/K20 (retained as general API-integration lessons).

**Scaffolding retained from the arc:**
- `MusicProviderRouter` (`@MainActor final`) — single-provider wrapper today, ready for a future provider
- `MusicProviderService` protocol — minus `handleAuthCallback(_:)` and `validateAuthorization()` (both Spotify-shaped)
- `ProviderAuthStatus` enum
- `ProviderArtwork` enum — only `.musicKit` case populated; URL-based case remains a plausible future shape
- `PlayableItem.id` provider-namespaced format (`track-appleMusic-<id>`)
- `LibrarySectionCache` provider-scoped keys
